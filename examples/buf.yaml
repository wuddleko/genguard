# Copy to your repo as genguard.yaml (usually next to buf.yaml).
# Requires: buf CLI — https://buf.build/docs/installation

groups:
  - name: protobuf
    command: buf generate
    outputs:
      - gen/
