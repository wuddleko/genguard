# Copy to your repo as genguard.yaml (next to sqlc.yaml).
# Requires: sqlc — https://docs.sqlc.dev/en/latest/overview/install.html

groups:
  - name: sqlc
    command: sqlc generate
    outputs:
      - internal/db/
