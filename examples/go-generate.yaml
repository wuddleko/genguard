# Copy to your repo as genguard.yaml (next to the files you generate).
# Requires: Go toolchain with //go:generate directives in your module.

groups:
  - name: go-generate
    command: go generate ./...
    outputs:
      - internal/generated/
