# Next release

`README.md` and `docs/ci.md` already include the text through the `--since` release. Put the manual for the following release here, then fold it into both files when that release is cut.
